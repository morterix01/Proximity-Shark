/// [pc]        → target Windows/Linux/Mac con tastiera italiana
/// [android]    → target Android con decodifica US QWERTY (default Android)
/// [androidIt]  → target Android con tastiera italiana impostata (es. samsungs con IT layout)
enum KeyboardLayout { pc, android, androidIt }
